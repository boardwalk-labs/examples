# Copy to .env (gitignored). `boardwalk dev` resolves permissions.secrets from this file;
# on hosted Boardwalk, add the secret in the dashboard instead.

# A GitHub token with read access to your issues (a fine-grained PAT with Issues: read works).
GITHUB_TOKEN=github_pat_…
