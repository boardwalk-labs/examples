# Copy to .env (gitignored). `boardwalk dev` resolves meta.secrets from this file;
# on Boardwalk Cloud, set the secret in the dashboard instead.

# Your Anthropic API key — used by the Claude Code subprocess.
ANTHROPIC_API_KEY=sk-ant-…
